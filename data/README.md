# Dataset

This project uses the NASA C-MAPSS Turbofan Engine Degradation Simulation Dataset.

The experiments are based on the **FD001** subset.

The dataset itself is not included in this repository.

## Required Files

The notebook expects the original `CMAPSSData.zip` archive.

For local execution, place the file in:

```text
data/CMAPSSData.zip